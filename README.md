# is-positive
A node module that returns true if the number is +ve

## Usage

```js
var isPositive = require('is-positive');

isPositive(0);
//=> false

isPositive(1);
//=> true

isPositive(-1);
//=> false
```
